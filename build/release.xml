<?xml version="1.0" encoding="UTF-8"?>
<!-- Generated by PHP Project Wizard (PPW) 1.0.4 on Mon Oct 3 15:21:39 CDT 2011 -->

<project name="release" default="build" basedir=".">
    <property name="source" value="${basedir}/src"/>
    <property name="release" value="" />
    <property name="versionfile" value="${basedir}/src/VERSION.TXT" />
    <exec executable="grep" failonerror="true"
        outputproperty="version" errorproperty="versionerror">
        <arg line='-o -e "[0-9]\{1,\}\.[0-9]\{1,\}\.[0-9]\{1,\}" ${versionfile}'
            />
    </exec>
    <property name="buildname" value="HUGnetLib-${version}" />
    <property name="staging" value="${basedir}/build/"/>
    <property name="builddir" value="${basedir}/build/rel/" />
    <property name="buildtarget" value="${builddir}/${buildname}" />

    <target name="clean" description="Clean up and create artifact directories">
        <delete dir="${staging}" failonerror="false"/>
        <delete failonerror="false">
            <fileset dir="${builddir}" includes="*.tar"/>
            <fileset dir="${builddir}" includes="*.tar.gz"/>
            <fileset dir="${builddir}" includes="*.tar."/>
        </delete>
    </target>

    <target name="setup" description="Copy all of the files to build the deb">
        <echo>Setting up for HUGnetLib 'v${version}'</echo>
        <mkdir dir="${staging}/${buildname}"/>
        <copy todir="${staging}/${buildname}">
            <fileset dir="${source}"/>
        </copy>
    </target>
    <target name="release" description="build the release files">
        <tar destfile="${buildtarget}.tar.gz"
            basedir="${staging}" compression="gzip"/>
        <checksum file="${buildtarget}.tar.gz" todir="${builddir}"
                        algorithm="sha1" format="MD5SUM"/>
        <checksum file="${buildtarget}.tar.gz" todir="${builddir}"
                        algorithm="md5" format="MD5SUM"/>
        <tar destfile="${buildtarget}.tar.bz2"
            basedir="${staging}" compression="bzip2"/>
        <checksum file="${buildtarget}.tar.bz2" todir="${builddir}"
                        algorithm="sha1" format="MD5SUM"/>
        <checksum file="${buildtarget}.tar.bz2" todir="${builddir}"
                        algorithm="md5" format="MD5SUM"/>
        <zip destfile="${buildtarget}.zip"
            basedir="${staging}"/>
        <checksum file="${buildtarget}.zip" todir="${builddir}"
                        algorithm="sha1" format="MD5SUM"/>
        <checksum file="${buildtarget}.zip" todir="${builddir}"
                        algorithm="md5" format="MD5SUM"/>
    </target>
    <target name="postclean" description="build the deb">
        <delete dir="${staging}"/>
    </target>
    <target name="build" depends="clean,setup,release,postclean"/>
</project>
